Order of operations:
        Create components for each element type (h1,button) - used one for simplicity (turned buttons comp into overall comp)
        Create a h1 element to pass information to
        create 3 buttons (start,pause,restart)
        utilizing the buttons class component include a function that increments numbers by one on click
        pass that function to the start button
        utilizing the buttons class component include a function that holds incrementation on click
        pass that function to the pause button
        utilizing the buttons class component include a function that refreshes the page on click that include the preventdefault function
        pass that function to the restart button